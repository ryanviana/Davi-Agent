# Copie para .env.local e preencha.
OPENAI_API_KEY=

# roteiro = demo mockada de hoje, zero rede (padrao)
# sombra  = infra roda de verdade, tela continua mockada, saida no console
# vivo    = a tela mostra o que a infra gerou
VITE_MOTOR=roteiro
